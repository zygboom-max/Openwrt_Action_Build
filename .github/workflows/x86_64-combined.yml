name: X86_64 Combined Parallel Build

on:
  workflow_dispatch:
    inputs:
      source:
        description: '选择要构建的源码类型'
        required: true
        default: 'both'
        type: choice
        options:
        - both
        - lede
        - immortalwrt
      build_type:
        description: '选择构建类型'
        required: false
        default: 'both'
        type: choice
        options:
        - both
        - Full
        - Mini
  schedule:
    - cron: 0 16 */2 * *

env:
  # 共享环境变量
  DIY_FEEDS: diy-feeds.sh
  PATCH_SCRIPT: patch/fix_version_error.sh
  INIT_SCRIPT: patch/init-settings.sh
  MAKE_SCRIPT: scripts/retry-make.sh
  CACHE_TOOLCHAIN: true
  UPLOAD_BIN_DIR: false
  FIRMWARE_RELEASE: true
  TZ: Asia/Shanghai

jobs:
  # LEDE 构建流程
  prepare_lede:
    if: ${{ github.event.inputs.source == 'both' || github.event.inputs.source == 'lede' }}
    runs-on: ubuntu-22.04
    name: 准备 LEDE 编译环境

    outputs:
      commit_author: ${{ steps.git_info.outputs.commit_author }}
      commit_date: ${{ steps.git_info.outputs.commit_date }}
      commit_message: ${{ steps.git_info.outputs.commit_message }}
      commit_hash: ${{ steps.git_info.outputs.commit_hash }}

    steps:
      - name: 合并磁盘
        uses: easimon/maximize-build-space@master
        with:
          swap-size-mb: 1024
          temp-reserve-mb: 100
          root-reserve-mb: 1024

      - name: 磁盘信息
        run: |
          set -xe
          df -H

      - name: 检查
        uses: actions/checkout@main

      - name: 克隆并获取源码信息
        id: git_info
        env:
          REPO_URL: https://github.com/coolsnowwolf/lede.git
          REPO_BRANCH: master
        run: |
          df -hT $GITHUB_WORKSPACE
          git clone -b $REPO_BRANCH --single-branch --filter=blob:none $REPO_URL openwrt
          cd openwrt
          COMMIT_AUTHOR=$(git show -s --date=short --format="作者: %an")
          echo "commit_author=$COMMIT_AUTHOR" >> $GITHUB_OUTPUT
          COMMIT_DATE=$(git show -s --date=short --format="时间: %ci")
          echo "commit_date=$COMMIT_DATE" >> $GITHUB_OUTPUT
          COMMIT_MESSAGE=$(git show -s --date=short --format="内容: %s")
          echo "commit_message=$COMMIT_MESSAGE" >> $GITHUB_OUTPUT
          COMMIT_HASH=$(git show -s --date=short --format="hash: %H")
          echo "commit_hash=$COMMIT_HASH" >> $GITHUB_OUTPUT

  build_lede:
    if: ${{ github.event.inputs.source == 'both' || github.event.inputs.source == 'lede' }}
    needs: prepare_lede
    runs-on: ubuntu-22.04
    name: 编译 LEDE ${{ matrix.build_type }} 版本固件

    strategy:
      matrix:
        build_type: ${{ fromJSON(github.event.inputs.build_type == 'both' && '["Full", "Mini"]' || github.event.inputs.build_type == 'Full' && '["Full"]' || '["Mini"]') }}
      max-parallel: 2
      fail-fast: false

    steps:
      - name: 设置配置文件路径
        id: set_config
        run: |
          if [ "${{ matrix.build_type }}" = "Full" ]; then
            echo "CONFIG_FILE=configs/x86_64.config" >> $GITHUB_ENV
          else
            echo "CONFIG_FILE=configs/x86_64-mini.config" >> $GITHUB_ENV
          fi

      - name: 检查机器性能
        run: |
          echo "警告⚠"
          echo "分配的服务器性能有限，若选择的插件过多，务必注意CPU性能！"
          echo -e "已知CPU型号(降序): 7763，8370C，8272CL，8171M，E5-2673\n"
          echo "--------------------------CPU信息--------------------------"
          echo "CPU物理数量: $(cat /proc/cpuinfo | grep "physical id" | sort | uniq | wc -l)"
          echo "CPU核心数量: $(nproc)"
          echo -e "CPU型号信息:$(cat /proc/cpuinfo | grep -m1 name | awk -F: '{print $2}')\n"
          echo "--------------------------内存信息--------------------------"
          echo "已安装内存详细信息:"
          echo -e "$(sudo lshw -short -C memory | grep GiB)\n"
          echo "--------------------------硬盘信息--------------------------"
          echo "硬盘数量: $(ls /dev/sd* | grep -v [1-9] | wc -l)" && df -hT

      - name: 释放磁盘空间
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: 初始化环境
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E apt-get -y update
          sudo -E systemctl daemon-reload
          sudo -E apt-get -y autoremove --purge
          sudo -E apt-get -y clean
          sudo -E apt-get -y install python3-socks python3-unidecode scdoc ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache clang cmake cpio curl device-tree-compiler flex gawk gcc-multilib g++-multilib gettext genisoimage git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool llvm lrzsz msmtp ninja-build p7zip p7zip-full patch pkgconf python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
          sudo timedatectl set-timezone "$TZ"

      - name: 合并磁盘
        uses: easimon/maximize-build-space@master
        with:
          swap-size-mb: 1024
          temp-reserve-mb: 100
          root-reserve-mb: 1024

      - name: 检查
        uses: actions/checkout@main

      - name: 克隆仓库
        env:
          REPO_URL: https://github.com/coolsnowwolf/lede.git
          REPO_BRANCH: master
        run: |
          chmod +x $GITHUB_WORKSPACE/scripts/*.sh
          chmod +x $GITHUB_WORKSPACE/patch/*.sh
          chmod +x $GITHUB_WORKSPACE/diy-*.sh
          df -hT $GITHUB_WORKSPACE
          git clone -b $REPO_BRANCH --single-branch --filter=blob:none $REPO_URL openwrt
          echo "OPENWRT_PATH=$PWD/openwrt" >> $GITHUB_ENV
          echo REPO_URL=$REPO_URL >> $GITHUB_ENV

      - name: 生成编译变量
        env:
          CONFIG_FILE: ${{ env.CONFIG_FILE }}
        run: |
          cd $OPENWRT_PATH
          cp $GITHUB_WORKSPACE/$CONFIG_FILE .config
          make defconfig > /dev/null 2>&1
          SOURCE_REPO="$(echo $REPO_URL | awk -F '/' '{print $(NF)}')"
          echo "SOURCE_REPO=$SOURCE_REPO" >> $GITHUB_ENV
          DEVICE_TARGET=$(cat .config | grep CONFIG_TARGET_BOARD | awk -F '"' '{print $2}')
          echo "DEVICE_TARGET=$DEVICE_TARGET" >> $GITHUB_ENV
          DEVICE_SUBTARGET=$(cat .config | grep CONFIG_TARGET_SUBTARGET | awk -F '"' '{print $2}')
          echo "DEVICE_SUBTARGET=$DEVICE_SUBTARGET" >> $GITHUB_ENV
          if grep -q "^CONFIG_USE_APK=y$" .config; then
              echo "USE_APK=true" >> $GITHUB_ENV
          else
              echo "USE_APK=false" >> $GITHUB_ENV
          fi

      - name: 缓存工具链
        if: env.CACHE_TOOLCHAIN == 'true'
        uses: HiGarfield/cachewrtbuild@main
        with:
          ccache: false
          mixkey: ${{ env.SOURCE_REPO }}-${{ env.REPO_BRANCH }}-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}-${{ matrix.build_type }}
          prefix: ${{ env.OPENWRT_PATH }}

      - name: 安装 Feed 并加载补丁
        run: |
          cd $OPENWRT_PATH
          $GITHUB_WORKSPACE/patch/update_eth.sh
          $GITHUB_WORKSPACE/$DIY_FEEDS
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 加载自定义配置
        env:
          CONFIG_FILE: ${{ env.CONFIG_FILE }}
        run: |
          [ -e files ] && rm -rf files
          [ -e $GITHUB_WORKSPACE/files ] && cp -r $GITHUB_WORKSPACE/files $OPENWRT_PATH/files
          cd $OPENWRT_PATH
          cp $GITHUB_WORKSPACE/$CONFIG_FILE .config
          $GITHUB_WORKSPACE/${{ matrix.build_type == 'Full' && 'diy-full.sh' || 'diy-mini.sh' }}

      - name: 下载依赖
        run: |
          cd $OPENWRT_PATH
          make defconfig
          $GITHUB_WORKSPACE/$MAKE_SCRIPT download
          find dl -size -1024c -exec ls -l {} \;
          find dl -size -1024c -exec rm -f {} \;

      - name: 编译固件 [${{ matrix.build_type }}]
        id: compile_lede
        run: |
          cd $OPENWRT_PATH
          mkdir -p files/etc/uci-defaults
          cp $GITHUB_WORKSPACE/$INIT_SCRIPT files/etc/uci-defaults/99-init-settings
          echo -e "$(nproc) thread compile"
          $GITHUB_WORKSPACE/$MAKE_SCRIPT nproc || make -j$(nproc) V=s
          echo "status=success" >> $GITHUB_OUTPUT
          echo "DATE=$(date +"%Y-%m-%d %H:%M:%S")" >> $GITHUB_ENV
          echo "FILE_DATE=$(date +"%Y.%m.%d")" >> $GITHUB_ENV

      - name: 检查磁盘空间
        if: (!cancelled())
        run: df -hT

      - name: 整理 ${{ matrix.build_type }} 版本固件
        if: steps.compile_lede.outputs.status == 'success'
        run: |
          cd $OPENWRT_PATH/bin/targets/*/*
          cat sha256sums
          cp $OPENWRT_PATH/.config build-${{ matrix.build_type }}.config
          if [ "${{ env.USE_APK }}" = "true" ]; then
            mv -f $OPENWRT_PATH/bin/packages/*/*/*.apk packages 2>/dev/null || true
          else
            mv -f $OPENWRT_PATH/bin/packages/*/*/*.ipk packages 2>/dev/null || true
          fi
          tar -zcf Packages-${{ matrix.build_type }}.tar.gz packages 2>/dev/null || true
          rm -rf packages feeds.buildinfo version.buildinfo
          echo "FIRMWARE_PATH=$PWD" >> $GITHUB_ENV
          echo "BUILD_TYPE=${{ matrix.build_type }}" >> $GITHUB_ENV

      - name: 保存固件至临时目录 (区分版本命名)
        if: steps.compile_lede.outputs.status == 'success'
        run: |
          echo "FIRMWARE_PATH: ${{ env.FIRMWARE_PATH }}"
          echo "检查源目录内容:"
          ls -lah ${{ env.FIRMWARE_PATH }} || echo "源目录不存在"

          mkdir -p $GITHUB_WORKSPACE/firmware/lede/${{ matrix.build_type }}
          if [ -d "${{ env.FIRMWARE_PATH }}" ]; then
            cd ${{ env.FIRMWARE_PATH }}
            count=0
            for file in *; do
              if [ -f "$file" ]; then
                mv "$file" "$GITHUB_WORKSPACE/firmware/lede/${{ matrix.build_type }}/${{ matrix.build_type }}_$file"
                count=$((count+1))
                echo "已移动: $file"
              fi
            done
            echo "共移动 $count 个文件"
          else
            echo "错误: FIRMWARE_PATH 目录不存在"
            exit 1
          fi

          echo "目标目录内容:"
          ls -lah $GITHUB_WORKSPACE/firmware/lede/${{ matrix.build_type }} || echo "目标目录为空"

      - name: 上传固件至 Artifact (用于多 Job 间传输)
        if: steps.compile_lede.outputs.status == 'success' && env.FIRMWARE_RELEASE == 'true'
        uses: actions/upload-artifact@main
        with:
          name: firmware-lede-${{ matrix.build_type }}
          path: ${{github.workspace}}/firmware/lede/${{ matrix.build_type }}
          retention-days: 1

  release_lede:
    if: always() && (github.event.inputs.source == 'both' || github.event.inputs.source == 'lede')
    needs: [prepare_lede, build_lede]
    runs-on: ubuntu-22.04
    name: 发布 LEDE 固件至 Release

    steps:
      - name: 检查 Build 任务状态
        run: |
          if [ "${{ needs.build_lede.result }}" != "success" ]; then
            echo "Build 任务失败，跳过发布"
            exit 1
          fi

      - name: 下载 Full 版本固件
        if: github.event.inputs.build_type == 'both' || github.event.inputs.build_type == 'Full'
        uses: actions/download-artifact@main
        with:
          name: firmware-lede-Full
          path: ./firmware-all

      - name: 下载 Mini 版本固件
        if: github.event.inputs.build_type == 'both' || github.event.inputs.build_type == 'Mini'
        uses: actions/download-artifact@main
        with:
          name: firmware-lede-Mini
          path: ./firmware-all

      - name: 整理固件文件
        run: |
          cd $GITHUB_WORKSPACE/firmware-all
          ls -lah
          echo "FILE_DATE=$(date +"%Y.%m.%d")" >> $GITHUB_ENV

          # Full 版本信息
          if [ -f "Full_openwrt-x86-64-generic.manifest" ] && [ -f "Full_profiles.json" ]; then
            echo "FULL_KERNEL_VERSION=$(grep "^kernel " Full_openwrt-x86-64-generic.manifest | awk '{split($3, a, "-"); print a[1]"-"a[2]}')" >> $GITHUB_ENV
            echo "FULL_FIRMWARE_VERSION=$(jq -r '.version_number // "unknown"' Full_profiles.json)" >> $GITHUB_ENV
          else
            echo "FULL_KERNEL_VERSION=未构建" >> $GITHUB_ENV
            echo "FULL_FIRMWARE_VERSION=未构建" >> $GITHUB_ENV
          fi

          # Mini 版本信息
          if [ -f "Mini_openwrt-x86-64-generic.manifest" ] && [ -f "Mini_profiles.json" ]; then
            echo "MINI_KERNEL_VERSION=$(grep "^kernel " Mini_openwrt-x86-64-generic.manifest | awk '{split($3, a, "-"); print a[1]"-"a[2]}')" >> $GITHUB_ENV
            echo "MINI_FIRMWARE_VERSION=$(jq -r '.version_number // "unknown"' Mini_profiles.json)" >> $GITHUB_ENV
          else
            echo "MINI_KERNEL_VERSION=未构建" >> $GITHUB_ENV
            echo "MINI_FIRMWARE_VERSION=未构建" >> $GITHUB_ENV
          fi

      - name: 发布固件至 Release (统一)
        uses: softprops/action-gh-release@v2
        with:
          name: R${{ env.FILE_DATE }}_LEDE_Combined
          tag_name: R${{ env.FILE_DATE }}_LEDE_Combined
          token: ${{ secrets.GH_TOKEN }}
          files: ${{ github.workspace }}/firmware-all/*
          body: |
            **OpenWrt LEDE Combined Firmware (Parallel Build)**
            ### 📒 固件信息
            - 💻 平台架构: x86_64
            - 📦 编译版本: Full + Mini 组合版本
            - ⚽ 固件源码: LEDE
            - 💝 源码分支: master
            - 内核版本:
              - Full: ${{ env.FULL_KERNEL_VERSION }}
              - Mini: ${{ env.MINI_KERNEL_VERSION }}
            - 🌐 默认地址: 192.168.1.1
            - 🔑 默认密码: 无
            ### 📋 文件说明
            - **Full_*** : Full 完整版本固件
            - **Mini_*** : Mini 精简版本固件
            ### 🧊 固件版本信息
            - 固件编译前最后一次 LEDE 源码更新记录
            - ${{ needs.prepare_lede.outputs.commit_author }}
            - ${{ needs.prepare_lede.outputs.commit_date }}
            - ${{ needs.prepare_lede.outputs.commit_message }}
            - ${{ needs.prepare_lede.outputs.commit_hash }}

  # ImmortalWrt 构建流程
  prepare_immortalwrt:
    if: ${{ github.event.inputs.source == 'both' || github.event.inputs.source == 'immortalwrt' }}
    runs-on: ubuntu-22.04
    name: 准备 ImmortalWrt 编译环境

    outputs:
      commit_author: ${{ steps.git_info.outputs.commit_author }}
      commit_date: ${{ steps.git_info.outputs.commit_date }}
      commit_message: ${{ steps.git_info.outputs.commit_message }}
      commit_hash: ${{ steps.git_info.outputs.commit_hash }}

    steps:
      - name: 合并磁盘
        uses: easimon/maximize-build-space@master
        with:
          swap-size-mb: 1024
          temp-reserve-mb: 100
          root-reserve-mb: 1024

      - name: 磁盘信息
        run: |
          set -xe
          df -H

      - name: 检查
        uses: actions/checkout@main

      - name: 克隆并获取源码信息
        id: git_info
        env:
          REPO_URL: https://github.com/immortalwrt/immortalwrt
          REPO_BRANCH: master
        run: |
          df -hT $GITHUB_WORKSPACE
          git clone -b $REPO_BRANCH --single-branch --filter=blob:none $REPO_URL openwrt
          cd openwrt
          COMMIT_AUTHOR=$(git show -s --date=short --format="作者: %an")
          echo "commit_author=$COMMIT_AUTHOR" >> $GITHUB_OUTPUT
          COMMIT_DATE=$(git show -s --date=short --format="时间: %ci")
          echo "commit_date=$COMMIT_DATE" >> $GITHUB_OUTPUT
          COMMIT_MESSAGE=$(git show -s --date=short --format="内容: %s")
          echo "commit_message=$COMMIT_MESSAGE" >> $GITHUB_OUTPUT
          COMMIT_HASH=$(git show -s --date=short --format="hash: %H")
          echo "commit_hash=$COMMIT_HASH" >> $GITHUB_OUTPUT

  build_immortalwrt:
    if: ${{ github.event.inputs.source == 'both' || github.event.inputs.source == 'immortalwrt' }}
    needs: prepare_immortalwrt
    runs-on: ubuntu-22.04
    name: 编译 ImmortalWrt ${{ matrix.build_type }} 版本固件

    strategy:
      matrix:
        build_type: ${{ fromJSON(github.event.inputs.build_type == 'both' && '["Full", "Mini"]' || github.event.inputs.build_type == 'Full' && '["Full"]' || '["Mini"]') }}
      max-parallel: 2
      fail-fast: false

    steps:
      - name: 设置配置文件路径
        id: set_config
        run: |
          if [ "${{ matrix.build_type }}" = "Full" ]; then
            echo "CONFIG_FILE=configs/x86_64-immortalWrt.config" >> $GITHUB_ENV
          else
            echo "CONFIG_FILE=configs/x86_64-immortalWrt-mini.config" >> $GITHUB_ENV
          fi

      - name: 检查机器性能
        run: |
          echo "警告⚠"
          echo "分配的服务器性能有限，若选择的插件过多，务必注意CPU性能！"
          echo -e "已知CPU型号(降序): 7763，8370C，8272CL，8171M，E5-2673\n"
          echo "--------------------------CPU信息--------------------------"
          echo "CPU物理数量: $(cat /proc/cpuinfo | grep "physical id" | sort | uniq | wc -l)"
          echo "CPU核心数量: $(nproc)"
          echo -e "CPU型号信息:$(cat /proc/cpuinfo | grep -m1 name | awk -F: '{print $2}')\n"
          echo "--------------------------内存信息--------------------------"
          echo "已安装内存详细信息:"
          echo -e "$(sudo lshw -short -C memory | grep GiB)\n"
          echo "--------------------------硬盘信息--------------------------"
          echo "硬盘数量: $(ls /dev/sd* | grep -v [1-9] | wc -l)" && df -hT

      - name: 释放磁盘空间
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: 初始化环境
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E apt-get -y update
          sudo -E systemctl daemon-reload
          sudo -E apt-get -y autoremove --purge
          sudo -E apt-get -y clean
          sudo -E apt-get -y install python3-socks python3-unidecode scdoc ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache clang cmake cpio curl device-tree-compiler flex gawk gcc-multilib g++-multilib gettext genisoimage git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool llvm lrzsz msmtp ninja-build p7zip p7zip-full patch pkgconf python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
          sudo timedatectl set-timezone "$TZ"

      - name: 合并磁盘
        uses: easimon/maximize-build-space@master
        with:
          swap-size-mb: 1024
          temp-reserve-mb: 100
          root-reserve-mb: 1024

      - name: 检查
        uses: actions/checkout@main

      - name: 克隆仓库
        env:
          REPO_URL: https://github.com/immortalwrt/immortalwrt
          REPO_BRANCH: master
        run: |
          chmod +x $GITHUB_WORKSPACE/scripts/*.sh
          chmod +x $GITHUB_WORKSPACE/patch/*.sh
          chmod +x $GITHUB_WORKSPACE/diy-*.sh
          df -hT $GITHUB_WORKSPACE
          git clone -b $REPO_BRANCH --single-branch --filter=blob:none $REPO_URL openwrt
          echo "OPENWRT_PATH=$PWD/openwrt" >> $GITHUB_ENV
          echo "REPO_URL=$REPO_URL" >> $GITHUB_ENV

      - name: 生成编译变量
        env:
          CONFIG_FILE: ${{ env.CONFIG_FILE }}
        run: |
          cd $OPENWRT_PATH
          cp $GITHUB_WORKSPACE/$CONFIG_FILE .config
          make defconfig > /dev/null 2>&1
          SOURCE_REPO="$(echo $REPO_URL | awk -F '/' '{print $(NF)}')"
          echo "SOURCE_REPO=$SOURCE_REPO" >> $GITHUB_ENV
          DEVICE_TARGET=$(cat .config | grep CONFIG_TARGET_BOARD | awk -F '"' '{print $2}')
          echo "DEVICE_TARGET=$DEVICE_TARGET" >> $GITHUB_ENV
          DEVICE_SUBTARGET=$(cat .config | grep CONFIG_TARGET_SUBTARGET | awk -F '"' '{print $2}')
          echo "DEVICE_SUBTARGET=$DEVICE_SUBTARGET" >> $GITHUB_ENV
          if grep -q "^CONFIG_USE_APK=y$" .config; then
              echo "USE_APK=true" >> $GITHUB_ENV
          else
              echo "USE_APK=false" >> $GITHUB_ENV
          fi

      - name: 缓存工具链
        if: env.CACHE_TOOLCHAIN == 'true'
        uses: HiGarfield/cachewrtbuild@main
        with:
          ccache: false
          mixkey: ${{ env.SOURCE_REPO }}-${{ env.REPO_BRANCH }}-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}-${{ matrix.build_type }}
          prefix: ${{ env.OPENWRT_PATH }}

      - name: 安装 Feed 并加载补丁
        run: |
          cd $OPENWRT_PATH
          $GITHUB_WORKSPACE/patch/update_eth.sh
          $GITHUB_WORKSPACE/$DIY_FEEDS
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 加载自定义配置
        env:
          CONFIG_FILE: ${{ env.CONFIG_FILE }}
        run: |
          [ -e files ] && rm -rf files
          [ -e $GITHUB_WORKSPACE/files ] && cp -r $GITHUB_WORKSPACE/files $OPENWRT_PATH/files
          cd $OPENWRT_PATH
          cp $GITHUB_WORKSPACE/$CONFIG_FILE .config
          $GITHUB_WORKSPACE/${{ matrix.build_type == 'Full' && 'diy-full.sh' || 'diy-mini.sh' }}

      - name: 下载依赖
        run: |
          cd $OPENWRT_PATH
          make defconfig
          $GITHUB_WORKSPACE/$MAKE_SCRIPT download
          find dl -size -1024c -exec ls -l {} \;
          find dl -size -1024c -exec rm -f {} \;

      - name: 编译固件 [${{ matrix.build_type }}]
        id: compile_immortalwrt
        run: |
          cd $OPENWRT_PATH
          mkdir -p files/etc/uci-defaults
          cp $GITHUB_WORKSPACE/$INIT_SCRIPT files/etc/uci-defaults/99-init-settings
          echo -e "$(nproc) thread compile"
          $GITHUB_WORKSPACE/$MAKE_SCRIPT nproc || make -j$(nproc) V=s
          echo "status=success" >> $GITHUB_OUTPUT
          echo "DATE=$(date +"%Y-%m-%d %H:%M:%S")" >> $GITHUB_ENV
          echo "FILE_DATE=$(date +"%Y.%m.%d")" >> $GITHUB_ENV

      - name: 检查磁盘空间
        if: (!cancelled())
        run: df -hT

      - name: 整理 ${{ matrix.build_type }} 版本固件
        if: steps.compile_immortalwrt.outputs.status == 'success'
        run: |
          cd $OPENWRT_PATH/bin/targets/*/*
          cat sha256sums
          cp $OPENWRT_PATH/.config build-${{ matrix.build_type }}.config
          if [ "${{ env.USE_APK }}" = "true" ]; then
            mv -f $OPENWRT_PATH/bin/packages/*/*/*.apk packages 2>/dev/null || true
          else
            mv -f $OPENWRT_PATH/bin/packages/*/*/*.ipk packages 2>/dev/null || true
          fi
          tar -zcf Packages-${{ matrix.build_type }}.tar.gz packages 2>/dev/null || true
          rm -rf packages feeds.buildinfo version.buildinfo
          echo "FIRMWARE_PATH=$PWD" >> $GITHUB_ENV
          echo "BUILD_TYPE=${{ matrix.build_type }}" >> $GITHUB_ENV

      - name: 保存固件至临时目录 (区分版本命名)
        if: steps.compile_immortalwrt.outputs.status == 'success'
        run: |
          echo "FIRMWARE_PATH: ${{ env.FIRMWARE_PATH }}"
          echo "检查源目录内容:"
          ls -lah ${{ env.FIRMWARE_PATH }} || echo "源目录不存在"

          mkdir -p $GITHUB_WORKSPACE/firmware/immortalwrt/${{ matrix.build_type }}
          if [ -d "${{ env.FIRMWARE_PATH }}" ]; then
            cd ${{ env.FIRMWARE_PATH }}
            count=0
            for file in *; do
              if [ -f "$file" ]; then
                mv "$file" "$GITHUB_WORKSPACE/firmware/immortalwrt/${{ matrix.build_type }}/${{ matrix.build_type }}_$file"
                count=$((count+1))
                echo "已移动: $file"
              fi
            done
            echo "共移动 $count 个文件"
          else
            echo "错误: FIRMWARE_PATH 目录不存在"
            exit 1
          fi

          echo "目标目录内容:"
          ls -lah $GITHUB_WORKSPACE/firmware/immortalwrt/${{ matrix.build_type }} || echo "目标目录为空"

      - name: 上传固件至 Artifact (用于多 Job 间传输)
        if: steps.compile_immortalwrt.outputs.status == 'success' && env.FIRMWARE_RELEASE == 'true'
        uses: actions/upload-artifact@main
        with:
          name: firmware-immortalwrt-${{ matrix.build_type }}
          path: ${{github.workspace}}/firmware/immortalwrt/${{ matrix.build_type }}
          retention-days: 1

  release_immortalwrt:
    if: always() && (github.event.inputs.source == 'both' || github.event.inputs.source == 'immortalwrt')
    needs: [prepare_immortalwrt, build_immortalwrt]
    runs-on: ubuntu-22.04
    name: 发布 ImmortalWrt 固件至 Release

    steps:
      - name: 检查 Build 任务状态
        run: |
          if [ "${{ needs.build_immortalwrt.result }}" != "success" ]; then
            echo "Build 任务失败，跳过发布"
            exit 1
          fi

      - name: 下载 Full 版本固件
        if: github.event.inputs.build_type == 'both' || github.event.inputs.build_type == 'Full'
        uses: actions/download-artifact@main
        with:
          name: firmware-immortalwrt-Full
          path: ./firmware-all

      - name: 下载 Mini 版本固件
        if: github.event.inputs.build_type == 'both' || github.event.inputs.build_type == 'Mini'
        uses: actions/download-artifact@main
        with:
          name: firmware-immortalwrt-Mini
          path: ./firmware-all

      - name: 整理固件文件
        run: |
          cd $GITHUB_WORKSPACE/firmware-all
          ls -lah
          echo "FILE_DATE=$(date +"%Y.%m.%d")" >> $GITHUB_ENV

          # Full 版本信息
          if [ -f "Full_profiles.json" ]; then
            echo "FULL_KERNEL_VERSION=$(jq -r '.linux_kernel.version // "unknown"' Full_profiles.json)" >> $GITHUB_ENV
            echo "FULL_FIRMWARE_VERSION=$(jq -r '.version_number // "unknown"' Full_profiles.json)" >> $GITHUB_ENV
          else
            echo "FULL_KERNEL_VERSION=未构建" >> $GITHUB_ENV
            echo "FULL_FIRMWARE_VERSION=未构建" >> $GITHUB_ENV
          fi

          # Mini 版本信息
          if [ -f "Mini_profiles.json" ]; then
            echo "MINI_KERNEL_VERSION=$(jq -r '.linux_kernel.version // "unknown"' Mini_profiles.json)" >> $GITHUB_ENV
            echo "MINI_FIRMWARE_VERSION=$(jq -r '.version_number // "unknown"' Mini_profiles.json)" >> $GITHUB_ENV
          else
            echo "MINI_KERNEL_VERSION=未构建" >> $GITHUB_ENV
            echo "MINI_FIRMWARE_VERSION=未构建" >> $GITHUB_ENV
          fi

      - name: 发布固件至 Release (统一)
        uses: softprops/action-gh-release@v2
        with:
          name: R${{ env.FILE_DATE }}_ImmortalWrt_Combined
          tag_name: R${{ env.FILE_DATE }}_ImmortalWrt_Combined
          token: ${{ secrets.GH_TOKEN }}
          files: ${{ github.workspace }}/firmware-all/*
          body: |
            **OpenWrt ImmortalWrt Combined Firmware (Parallel Build)**
            ### 📒 固件信息
            - 💻 平台架构: x86_64
            - 📦 编译版本: Full + Mini 组合版本
            - ⚽ 固件源码: ImmortalWrt
            - 💝 源码分支: master
            - 内核版本
              - Full: ${{ env.FULL_KERNEL_VERSION }}
              - Mini: ${{ env.MINI_KERNEL_VERSION }}
            - 🌐 默认地址: 192.168.1.1
            - 🔑 默认密码: 无
            ### 📋 文件说明
            - **Full_*** : Full 完整版本固件
            - **Mini_*** : Mini 精简版本固件
            ### 🧊 固件版本信息
            - 固件版本:
              - Full: ${{ env.FULL_FIRMWARE_VERSION }}
              - Mini: ${{ env.MINI_FIRMWARE_VERSION }}
            - 固件编译前最后一次 ImmortalWrt 源码更新记录
            - ${{ needs.prepare_immortalwrt.outputs.commit_author }}
            - ${{ needs.prepare_immortalwrt.outputs.commit_date }}
            - ${{ needs.prepare_immortalwrt.outputs.commit_message }}
            - ${{ needs.prepare_immortalwrt.outputs.commit_hash }}
